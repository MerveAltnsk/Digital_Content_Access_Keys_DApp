import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Digital Content Access Keys | Stellar DApp",
  description: "Secure, decentralized access management for digital content using Stellar blockchain",
  keywords: ["Stellar", "Soroban", "DApp", "Digital Content", "Access Keys", "Blockchain"],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <main className="min-h-screen">
          {children}
        </main>
      </body>
    </html>
  );
}